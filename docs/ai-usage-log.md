# AI usage log

Required by NISR hackathon rule 10 (AI use must be disclosed). Append one row per work session using `/log-ai-use`. Never delete rows.

**Note on rows marked "reconstructed from git history":** rule 9 asks for one row per AI-assisted
session, logged at the time via `/log-ai-use`. That did not happen for any session before
2026-09-23's compliance pass. Every row below covering work up to and including the district
crosswalk was backfilled in one pass on 2026-09-23 by reading `git log` and this session's own
record, not logged live session-by-session as intended. Going forward (see `CONTRIBUTING.md`),
`/log-ai-use` should be run at the end of each AI-assisted session instead.

| Date | Tool | Area | Task | Files touched | Human review |
|---|---|---|---|---|---|
| 2026-09-23 | Claude Code | `src/agritwin/gee/`, `data/external/boundaries/` | Initial project scaffold: pyproject, Makefile, config loaders, GEE district boundary fetch and extraction skeleton (reconstructed from git history) | `pyproject.toml`, `Makefile`, `config/*.yaml`, `src/agritwin/config.py`, `src/agritwin/gee/boundaries.py`, `src/agritwin/gee/extract.py` | Reconstructed from git history: commit message and diff reviewed by the author before merge (implied by direct commits to `main`, no PR queue per `CLAUDE.md`'s development method) |
| 2026-09-23 | Claude Code | `src/agritwin/gee/` | Fixed two real GEE bugs: GAUL property renaming before `getInfo()`, and using `Reducer.mean()` not `Reducer.sum()` for rainfall zonal stats (reconstructed from git history; commit messages document the bug and fix directly) | `src/agritwin/gee/boundaries.py`, `src/agritwin/gee/extract.py` | Reconstructed from git history: both fixes are regression-tested (`tests/test_gee_boundaries.py::test_raw_gaul_property_names_are_rejected`, `tests/test_gee_extract.py::test_rainfall_spatial_reducer_is_mean_not_sum`) |
| 2026-09-23 | Claude Code | `docs/` | Filled in team member names and contact emails in `ORIGINALITY.md`/`AI_DISCLOSURE.md` (reconstructed from git history) | `ORIGINALITY.md`, `AI_DISCLOSURE.md` | Reconstructed from git history: team-identifying details, necessarily supplied and confirmed by the team, not invented by the model |
| 2026-09-23 | Claude Code | `src/agritwin/features/` | Joined the four Earth Engine output CSVs into one district x season x year mart (reconstructed from git history) | `src/agritwin/features/gee_mart.py`, `src/agritwin/features/run.py`, `tests/test_gee_mart.py` | Reconstructed from git history: covered by `tests/test_gee_mart.py` (row-count preservation, broadcast join, no-null-keys) |
| 2026-09-23 | Claude Code | `api/` | Scaffolded a minimal FastAPI app with `/health` and `/geo/districts` (reconstructed from git history) | `api/app/main.py`, `api/app/data_store.py`, `api/app/schemas.py`, `api/app/settings.py`, `api/app/routers/geo.py`, `api/tests/test_geo.py` | Reconstructed from git history: verified working live in this session's own status audit (`uvicorn` started, both endpoints returned real data) |
| 2026-09-23 | Claude Code | `web/` | Scaffolded a React + Vite + TypeScript app with a MapLibre district map (reconstructed from git history) | `web/src/App.tsx`, `web/src/main.tsx`, `web/package.json`, `web/tsconfig.json`, `web/vite.config.ts` | Reconstructed from git history: **not fully reviewed/fixed** — this session's status audit (`docs/STATUS.md`) found `tsc`, `eslint` and `vitest` all currently fail against this scaffold; flagged as technical debt, not yet corrected |
| 2026-09-23 | Claude Code | `docs/data/variable_audit.md`, `config/sas_variable_map.yaml`, `docs/decisions.md` | `/audit-sas` runs for all 7 SAS years (2019-2025): column-to-canonical-variable mapping, confidence tagging, crop code counts, weight variable summaries; resolved the 2019 segment-level vs 2020+ plot-level weighting design question (with an explicit `survey-methodologist` subagent review) and the `harvest_kg` (s2q21 vs s2q22) source question via direct cross-tab evidence on real (unprinted, schema/summary-only) microdata | `config/sas_variable_map.yaml`, `docs/data/variable_audit.md`, `docs/decisions.md` | Human review documented directly in-session: multiple explicit user requests to verify subagent involvement was real (not restated model conclusions), to check individual per-year evidence rather than pattern inference, and to confirm the evidence chain between subagent-reviewed and post-review-confirmed statistics — all addressed and corrected in `docs/decisions.md` before being accepted |
| 2026-09-23 | Claude Code | `src/agritwin/harmonize/` | Implemented the harmonize module (join SAS years, rename to canonical schema, stack, confidence sidecar, value-label sidecar); user-identified and fixed a farmer_type/erosion_degree value-label preservation gap; logged the district crosswalk gap as a blocking open item | `src/agritwin/harmonize/*.py`, `tests/test_harmonize.py`, `tests/fixtures/synthetic_sas.py`, `docs/decisions.md` | Human review documented directly in-session: plan proposed and approved before implementation (per `/pipeline-step`); user requested and received confirmation of a real join-collision bug found during test-writing; user requested two additional tests (2019 weight sourcing, Season C exclusion) after initial implementation, both added; `make lint && make test` run and passing before each commit |
| 2026-09-23 | Claude Code | `Makefile`, `src/agritwin/CLAUDE.md`, `tests/`, `scripts/`, `api/app/settings.py` | Collapsed the never-built `ingest/` layer into `harmonize/`; fixed a missing `tests/__init__.py` breaking `pytest`; fixed pre-existing `ruff` errors in `scripts/`; fixed a `mypy` error in `api/app/settings.py` (`ConfigDict` vs `SettingsConfigDict`) | `Makefile`, `src/agritwin/CLAUDE.md`, `tests/__init__.py`, `scripts/check_public.py`, `scripts/test_gee.py`, `api/app/settings.py` | Human review documented directly in-session: each fix run through `make test`/`make lint`/`mypy` and confirmed clean before commit; user requested and confirmed each fix explicitly across three separate messages |
| 2026-09-23 | Claude Code | `docs/STATUS.md` | Read-only project status audit against `docs/AgriTwin_Master_Build_Guide.md` (inventory, pipeline/methodology/model/API/frontend status, compliance and security checks, deploy status, prioritized next steps) | `docs/STATUS.md` | Human review documented directly in-session: every claim cross-checked against the actual repo (file reads, `git log`, live `pytest`/`ruff`/`mypy`/`npm` runs, a live `uvicorn` start); one factual gap flagged explicitly (the audit's own referenced plan document does not exist in this repo) rather than guessed around |
| 2026-09-23 | Claude Code | `data/reference/`, `data/public/geo/`, `src/agritwin/gee/`, `src/agritwin/features/gee_mart.py`, `docs/decisions.md`, `docs/data-sources.md` | Built the SAS `district_code` <-> HDX/GAUL district crosswalk (downloaded from HDX, joined against SAS value labels for all 7 years), re-keyed existing GEE outputs, wired `gee/` extraction to a client-side HDX `FeatureCollection`, closed the open blocking item in `docs/decisions.md` | `data/reference/district_crosswalk.csv`, `data/public/geo/districts_adm2.geojson`, `docs/data-sources.md`, `src/agritwin/gee/boundaries.py`, `src/agritwin/gee/extract.py`, `src/agritwin/gee/run.py`, `src/agritwin/features/gee_mart.py`, `tests/test_district_crosswalk.py`, `tests/test_gee_boundaries.py`, `tests/test_gee_extract.py`, `tests/test_gee_mart.py`, `docs/decisions.md` | Human review documented directly in-session: crosswalk exactness verified against all 7 raw SAS years' value labels (not assumed); HDX extraction path verified with a live Earth Engine call, not just mocked tests; `make lint`/`pytest` run clean before each commit |
| 2026-09-23 | Claude Code | `docs/ai-usage-log.md`, `NOTICE`, `CONTRIBUTING.md` | Compliance backfill: this log, a `NOTICE` file (IP transfer + third-party data licenses), and a `CONTRIBUTING.md` rule requiring `ai-usage-log.md` updates in the same PR going forward | `docs/ai-usage-log.md`, `NOTICE`, `CONTRIBUTING.md` | Human review documented directly in-session: content drafted from facts already established in this repo (hackathon rule 10, licenses recorded in `config/data_sources.yaml` and `docs/data-sources.md`), not invented |
| 2026-09-23 | Claude Code | `src/agritwin/clean/`, `src/agritwin/survey/`, `docs/survey-design.md`, `docs/validation.md`, `docs/decisions.md` | Built `clean/` (yield_kg_ha, qc_flag; a prerequisite the task's own instructions surfaced as missing, confirmed with the team before building rather than assumed) and `survey/` (Taylor-linearized, samplics-based, design-based weighted district/province/national yield estimates), documented real design-variable issues found by reading raw value labels and row frequencies (stratum code instability across years, a 2021 unlabeled code resolved with evidence and team confirmation), and validated the national maize estimate against the real, live-fetched NISR SAS 2025B report | `src/agritwin/clean/*.py`, `src/agritwin/survey/*.py`, `config/settings.yaml`, `docs/survey-design.md`, `docs/validation.md`, `docs/decisions.md`, `tests/test_clean.py`, `tests/test_survey.py`, `tests/test_survey_run.py`, `tests/test_survey_validation.py`, `data/public/district_yield.csv` | Human review documented directly in-session: two explicit AskUserQuestion confirmations before implementation (build `clean/` first; treat 2021's unlabeled stratum code as equivalent to code 10) on points the task itself flagged as "stop and ask if ambiguous"; the official validation figure was fetched live from the real NISR report PDF, not recalled from memory; two real samplics library bugs found and worked around during a live run against the full real dataset, not just synthetic tests; `make lint`/`pytest` (81 tests) run clean before every commit |
| 2026-09-24 | Claude Code | `src/agritwin/gee/periods.py`, `data/external/gee/`, `data/public/drivers.csv`, `data/public/drivers_by_district.csv`, `data/public/nowcast_backtest.csv`, `docs/decisions.md` | External-review Step 1: confirmed and fixed a Season A date-alignment bug in GEE extraction (verified against the real NISR SAS 2025 Season A report before touching code, not assumed), re-pulled Season A satellite data only (scope confirmed with the team before spending Earth Engine quota), re-ran features join, drivers, and nowcast, reported before/after metrics | `src/agritwin/gee/periods.py`, `tests/test_gee_periods.py`, `data/external/gee/ndvi_district_season.csv`, `data/external/gee/rainfall_district_season.csv`, `data/external/gee/nowcast_features_district_season_year.csv`, `data/external/gee/feat_district_season_joined.csv`, `data/public/drivers.csv`, `data/public/drivers_by_district.csv`, `data/public/nowcast_backtest.csv`, `docs/decisions.md` | Human review documented directly in-session: root evidence was a live-fetched primary source (the actual NISR report), not an assumption; a second, self-inflicted bug (missing `gaul_district_code` on new rows, silently dropping half the joined feature mart) was caught by checking row counts against the expected total rather than assuming the re-pull succeeded; re-pull scope (Season A only, skip unaffected steps) explicitly confirmed with the team before spending Earth Engine quota; `make lint`/`pytest` (127 tests) run clean before commit; full before/after comparison reported and logged in `docs/decisions.md` before proceeding to Step 2, per the task's explicit checkpoint |
| 2026-09-24 | Claude Code | `src/agritwin/models/nowcast.py`, `src/agritwin/models/run.py`, `docs/nowcast-feature-timing.md`, `docs/decisions.md` | External-review Step 2: investigated and fixed prior-season-yield leakage in the nowcast, using real NISR publication dates (PDF creation timestamps of the actual SAS 2025A/2025B reports) rather than assuming realistic timing; chose option (b) (per-lead-time realistic lookback) per the team's decision; re-ran the backtest and reported before/after | `src/agritwin/models/nowcast.py`, `src/agritwin/models/run.py`, `tests/test_nowcast.py`, `docs/nowcast-feature-timing.md`, `docs/decisions.md`, `data/public/nowcast_backtest.csv` | Human review documented directly in-session: investigation findings (with real evidence, not estimates) presented and the team chose option (b) explicitly before any code changed; `make lint`/`pytest` (130 tests) run clean before commit; before/after backtest comparison confirmed no crop x lead-time combination flips which model wins |
| 2026-09-24 | Claude Code | `src/agritwin/models/nowcast.py`, `src/agritwin/models/run.py`, `docs/decisions.md` | External-review Step 3: investigated reliability-flag distribution per crop (found 48-73% suppression rates, worst for maize and irish_potato, matching the review's hunch), proposed exclusion with row counts shown before implementing, team approved, implemented, re-ran backtest | `src/agritwin/models/nowcast.py`, `src/agritwin/models/run.py`, `tests/test_nowcast.py`, `tests/test_models_run.py`, `docs/decisions.md`, `data/public/nowcast_backtest.csv` | Human review documented directly in-session: row counts (before/after per crop, per-fold minimums) shown and approach approved before any code changed, per the task's explicit checkpoint; `make lint`/`pytest` (132 tests) run clean before commit; before/after backtest comparison reported in full, including a real shift in which model wins for sorghum and beans, not just magnitude changes |
| 2026-09-24 | Claude Code | `src/agritwin/models/nowcast.py`, `src/agritwin/models/run.py`, `docs/decisions.md` | External-review Step 4: added MAE (kg/ha) and per-fold MAPE spread alongside MAPE, computed a rigorous paired per-fold delta (mean and std across the 7 held-out years, not just point estimates) for every crop x lead x model, wrote an honest per-crop verdict avoiding both an overstated "works" and an overstated "nothing works" summary | `src/agritwin/models/nowcast.py`, `src/agritwin/models/run.py`, `tests/test_nowcast.py`, `docs/decisions.md`, `data/public/nowcast_backtest.csv` | Human review documented directly in-session: the paired-delta analysis was computed directly from real model outputs on the real dataset, not summarized from the point estimates alone, specifically to avoid overclaiming or underclaiming per the task's explicit instruction; `make lint`/`pytest` (133 tests) run clean before commit |
| 2026-09-24 | Claude Code | `README.md`, `api/app/main.py`, `pyproject.toml`, `docs/AgriTwin_Master_Build_Guide.md`, GitHub repo settings | External-review Step 5: replaced the product tagline "Yield gap and early harvest estimator" with "Early-season crop yield intelligence and yield-gap analysis" everywhere except docs/decisions.md's historical entry (and the build guide's own decision-log-style entry, same reasoning); updated the GitHub repository's short description directly via `gh repo edit` | `README.md`, `api/app/main.py`, `pyproject.toml`, `docs/AgriTwin_Master_Build_Guide.md`, GitHub repo description (not a repo file) | Human review documented directly in-session: grepped for every occurrence before and after to confirm only the two explicitly-preserved historical entries remain; `make lint`/`pytest` (133 tests) run clean before commit |
| 2026-09-24 | Claude Code | `docs/STATUS.md`, `README.md` | External-review Step 6: rewrote docs/STATUS.md from scratch reflecting real current state (pulled from actual file listings, git log, and pytest, not memory), updated README's Methodology and validation section with three real validation tables pulled directly from docs/validation.md, artifacts/*_metadata.json, and data/public/nowcast_backtest.csv, labeled unbuilt features (scenario explorer, trilingual UI) as planned rather than implying they exist | `docs/STATUS.md`, `README.md` | Human review documented directly in-session: every number in the new validation tables cross-checked against the actual committed files immediately before writing, not invented or rounded favorably per the task's explicit instruction; `make lint`/`pytest` (133 tests) run clean before commit |
| 2026-09-24 | Claude Code | `LICENSE`, `NOTICE`, `docs/STATUS.md` | External-review Step 7: checked the actual NISR hackathon page live (not the search-engine summary, which was circular and echoed this repo's own content back) for any license constraint before proposing one; confirmed no constraint beyond the already-documented IP transfer; team confirmed MIT; added LICENSE and updated NOTICE/STATUS.md's now-resolved findings | `LICENSE`, `NOTICE`, `docs/STATUS.md` | Human review documented directly in-session: license choice explicitly confirmed via AskUserQuestion before finalizing, per the task's explicit instruction; the hackathon-rules check used a live WebFetch of the real NISR page, and a first search-engine result was explicitly distrusted and rejected as circular rather than treated as independent confirmation; `make lint`/`pytest` (133 tests) run clean before commit |
| 2026-09-24 | Claude Code | `api/app/routers/drivers.py`, `api/app/routers/backtest.py`, `api/app/schemas.py`, `api/app/data_store.py`, `api/app/main.py`, `docs/decisions.md` | Wired GET /drivers and GET /backtest onto real data/public/ CSVs; discovered while implementing (not before) that neither the driver model nor the nowcast backtest ever split by season, documented the gap rather than faking the filter; live-verified both endpoints against real data with a running uvicorn instance, not just unit tests | `api/app/routers/drivers.py`, `api/app/routers/backtest.py`, `api/app/schemas.py`, `api/app/data_store.py`, `api/app/main.py`, `api/tests/test_drivers.py`, `api/tests/test_backtest.py`, `api/tests/fixtures/data/public/*.csv`, `docs/decisions.md` | Human review documented directly in-session: real gap (season pooling) found and documented rather than silently worked around; live uvicorn + curl smoke test against the actual committed data confirmed correct behavior including a "suppressed" district row still returned, not hidden; `make lint`/`pytest` (143 tests) run clean before commit |
| 2026-09-24 | Claude Code | `web/tsconfig.json`, `web/src/main.tsx`, `web/src/App.tsx`, `web/src/vite-env.d.ts`, `web/.eslintrc.cjs`, `web/src/lib/format.ts`, `docs/STATUS.md`, `docs/decisions.md` | Fixed the web app's build/lint/test toolchain: all 3 previously-broken tsc errors, missing ESLint config, and zero test files. Verified with real `npm run lint/typecheck/build/test` and the repo-root `make lint && make test`, both fully green for the first time this session | `web/tsconfig.json`, `web/src/main.tsx`, `web/src/App.tsx`, `web/src/vite-env.d.ts`, `web/.eslintrc.cjs`, `web/src/lib/format.ts`, `web/src/lib/format.test.ts`, `docs/STATUS.md`, `docs/decisions.md` | Human review documented directly in-session: fixed each of the 3 real tsc errors with the standard/idiomatic fix (real GeoJSON types, not a type-check suppression) rather than loosening compiler strictness; new ESLint config and lib/format.ts both used only already-installed dependencies, no new packages added; `make lint && make test` run clean across the whole repo (Python and web) before commit |
| 2026-09-24 | Claude Code | `web/src/pages/MapPage.tsx`, `web/src/api/*`, `web/src/lib/colors.ts`, `web/src/lib/districtCrosswalk.ts`, `web/src/routes.tsx`, `web/src/main.tsx`, `docs/decisions.md` | Built the first real product page (district yield-gap choropleth map with crop/season/year filters, backed by the live `/districts` and `/yield-gap` endpoints); found and fixed a real district-code mismatch (GAUL vs NISR codes) between those two endpoints only by opening the page in an actual browser, not from `tsc`/`eslint`/`vitest` alone | `web/src/api/client.ts`, `web/src/api/types.ts`, `web/src/api/hooks.ts`, `web/src/pages/MapPage.tsx`, `web/src/lib/colors.ts`, `web/src/lib/colors.test.ts`, `web/src/lib/districtCrosswalk.ts`, `web/src/routes.tsx`, `web/src/main.tsx`, `web/src/App.css`, `docs/decisions.md` | Human review documented directly in-session: ran a real `uvicorn` against real `data/public/` files and a real `vite` dev server, opened it in Chrome, confirmed the choropleth colors, hover tooltip values, and crop-switch re-fetch all show real per-district numbers before considering the page done, per the system's explicit requirement to verify UI changes in a browser rather than claim success from type-checks and tests alone; `make lint && make test` (repo-wide) run clean before commit |
| 2026-09-24 | Claude Code | `api/app/data_store.py`, `api/app/schemas.py`, `api/tests/test_geo.py`, `web/src/api/types.ts`, `web/src/pages/MapPage.tsx`, `docs/decisions.md`, `docs/STATUS.md` | Closed the /districts-vs-model-endpoints district-code gap server-side (join `data/reference/district_crosswalk.csv` onto the boundaries GeoJSON once at load time, add `district_code`), removing the client-side crosswalk workaround from the previous session's map page | `api/app/data_store.py`, `api/app/schemas.py`, `api/tests/test_geo.py`, `web/src/api/types.ts`, `web/src/pages/MapPage.tsx` (deleted `web/src/lib/districtCrosswalk.ts`), `docs/decisions.md`, `docs/STATUS.md` | Human review documented directly in-session: re-ran the live browser check (real uvicorn + real vite dev server + real Chrome tab) against the changed server and frontend, confirmed the same correct choropleth as before with no client-side lookup table; `make lint && make test` (repo-wide) run clean before commit |
| 2026-09-24 | Claude Code | `src/agritwin/harmonize/core.py`, `src/agritwin/harmonize/run.py`, `tests/test_harmonize.py`, `docs/decisions.md`, `docs/STATUS.md` | Wired `district_name` into `stg_sas_plot_crop` via the existing crosswalk; investigated the companion `docs/STATUS.md` item (`features/gee_mart.py`'s SAS-side join) and found it has no consumer anywhere in the repo, so did not build speculative code for it, documented the finding instead | `src/agritwin/harmonize/core.py`, `src/agritwin/harmonize/run.py`, `tests/test_harmonize.py`, `docs/decisions.md`, `docs/STATUS.md` | Human review documented directly in-session: re-ran `make stage` against the real 2019-2025 SAS microdata (not just synthetic fixtures), confirmed all 513,369 rows across all 30 districts got a non-null `district_name` with zero unmatched codes, checked via aggregate `value_counts()`/`isna().sum()` only, never printing raw rows, per `CLAUDE.md` rule 2; `make lint && make test` (repo-wide, 145 tests) run clean before commit |
| 2026-09-24 | Claude Code | `src/agritwin/models/scenario.py`, `src/agritwin/models/run.py`, `tests/test_scenario.py`, `config/settings.yaml`, `tests/test_models_run.py`, `docs/decisions.md`, `docs/STATUS.md` | Built `models/scenario.py`: precomputed lever grid for the scenario explorer (16 lever configurations per district x crop, bootstrap CI), wired into `run_drivers()`, writes `data/public/scenario.csv`. Found the first implementation took 17m51s on real data (one LightGBM predict() call per bootstrap rep); rewrote to batch all reps into one predict() call per configuration, cutting real-data runtime to 1m53s with identical point estimates | `src/agritwin/models/scenario.py`, `src/agritwin/models/run.py`, `tests/test_scenario.py`, `tests/test_models_run.py`, `config/settings.yaml`, `docs/decisions.md`, `docs/STATUS.md` | Human review documented directly in-session: ran the real pipeline end to end on real 2019-2025 SAS/GEE data (not just synthetic fixtures) twice, before and after the vectorization fix, checked only aggregate stats (row counts, groupby means, min/max) per `CLAUDE.md` rule 2, confirmed `improved_seed=True` yields a consistently higher predicted yield than `False` (consistent with the already-validated SHAP ranking); `scripts/check_public.py` privacy check passed; `make lint && make test` (repo-wide, 149 tests) run clean before commit |
| 2026-09-24 | Claude Code | `api/app/routers/meta.py`, `api/app/routers/kpis.py`, `api/app/routers/districts.py`, `api/app/routers/scenario.py`, `api/app/data_store.py`, `api/app/main.py`, `api/app/schemas.py`, `docs/decisions.md`, `docs/STATUS.md` | Built the 4 remaining mechanical API endpoints (/meta, /kpis, /districts/{code}/profile, /scenario/{code}), all composites over data/public/ with no new modeling; found and fixed a real bug during live verification (pandas evaluates every operand of a combined boolean filter, so casting a mixed district/national geo_code column to float crashed before the geo_level filter could exclude the non-numeric "RWA" rows); explicitly did not stub /nowcast, /nowcast/{code}/curve, /briefs/{code}.pdf, which are genuinely blocked on missing modeling/export work | `api/app/routers/meta.py`, `api/app/routers/kpis.py`, `api/app/routers/districts.py`, `api/app/routers/scenario.py`, `api/app/data_store.py`, `api/app/main.py`, `api/app/schemas.py`, `api/tests/test_meta.py`, `api/tests/test_kpis.py`, `api/tests/test_districts.py`, `api/tests/test_scenario.py`, `api/tests/fixtures/data/public/district_yield.csv`, `api/tests/fixtures/data/public/district_zones.csv`, `api/tests/fixtures/data/public/scenario.csv`, `docs/decisions.md`, `docs/STATUS.md` | Human review documented directly in-session: ran a real uvicorn against real data/public/ for all four endpoints, spot-checked /kpis' national maize figure (1,400 kg/ha) against the already-validated number in docs/validation.md, confirmed all 8 real endpoints in /openapi.json; noticed unrelated uncommitted changes to data/external/gee/*.csv and data/public/district_yield.csv from outside this session and deliberately left them out of this commit rather than including or reverting them silently; `make lint && make test` (repo-wide, 163 tests) run clean before commit |
| 2026-09-24 | Claude Code | `src/agritwin/features/gee_mart.py`, `tests/test_gee_mart.py`, `docs/decisions.md`, plus refreshed `data/external/gee/*.csv` and `data/public/*.csv` | Fixed a real `make features` crash (user's own `make all` run hit `KeyError: 'gaul_district_code'`): `gee_mart.py`'s join keys were stale relative to `gee/run.py`'s current HDX-boundary-based extraction, and the module's own unit tests used synthetic fixtures that still had the now-nonexistent column, so they never caught the drift. Re-ran the rest of the user's `make all` (`make features`, `make train`, `scripts/check_public.py`) to confirm the full pipeline completes | `src/agritwin/features/gee_mart.py`, `tests/test_gee_mart.py`, `docs/decisions.md`, `data/external/gee/*.csv`, `data/public/*.csv` | Human review documented directly in-session: root-caused via the user's own pasted terminal output (not a hypothesis); re-ran the fixed step against the user's real freshly-regenerated GEE data, then continued `make train` and the privacy check to confirm the whole chain now completes end to end; new driver-model metrics on the refreshed data cross-checked against the previously-validated numbers (same direction and magnitude); `make lint && make test` (163 tests) run clean before commit |
| 2026-09-24 | Claude Code | `src/agritwin/export/geo.py`, `src/agritwin/export/briefs.py`, `src/agritwin/export/run.py`, `pyproject.toml`, `docs/decisions.md`, `docs/STATUS.md` | Built export/ per src/agritwin/CLAUDE.md's module responsibility: simplified/district_code-joined boundaries GeoJSON (6.25MB to 211KB) and one real PDF brief per district (30 total, jinja2 + weasyprint, both already-installed deps, no new dependency added). Explicit decision to keep survey/'s and models/'s existing direct-to-data/public/ writes rather than refactor them into export/, resolving the open question flagged in a previous STATUS.md. Found and fixed a real scope bug during verification: the first brief included a non-MVP crop (rice) because yield_gap.csv contains it even though config/settings.yaml's scope.crops does not | `src/agritwin/export/__init__.py`, `src/agritwin/export/geo.py`, `src/agritwin/export/briefs.py`, `src/agritwin/export/run.py`, `tests/test_export.py`, `pyproject.toml`, `docs/decisions.md`, `docs/STATUS.md`, plus `data/public/geo/rwanda_districts_simplified.geojson` and `data/public/briefs/*.pdf` | Human review documented directly in-session: ran the real export step against real data/public/ and data/external/boundaries/ data; read the actual rendered PDF bytes (not just "it didn't crash") for a real district and caught the rice-inclusion bug by inspecting real output; spot-checked the simplified GeoJSON's district_code join against the already-validated crosswalk value; scripts/check_public.py passed; make lint && make test (170 tests) run clean before commit |
| 2026-09-24 | Claude Code | `api/app/routers/briefs.py`, `api/app/main.py`, `docs/decisions.md`, `docs/STATUS.md` | Wired the last non-blocked API endpoint, GET /briefs/{code}.pdf, serving the precomputed PDFs from data/public/briefs/ built by export/ earlier this session. crop and season accepted per the documented signature but have no effect (each brief already covers all 4 crops on one page), same pattern as /drivers and /backtest's unused season param | `api/app/routers/briefs.py`, `api/app/main.py`, `api/tests/test_briefs.py`, `api/tests/fixtures/data/public/briefs/57.pdf`, `docs/decisions.md`, `docs/STATUS.md` | Human review documented directly in-session: ran a real uvicorn against real data/public/briefs/, confirmed a real PDF with the correct content-type is served, confirmed the {code}.pdf path pattern with a literal suffix works correctly in FastAPI, confirmed all 9 endpoints in /openapi.json; make lint && make test (175 tests) run clean before commit |
| 2026-09-24 | Claude Code | `src/agritwin/models/nowcast.py`, `src/agritwin/models/run.py`, `api/app/routers/nowcast.py`, `api/app/schemas.py`, `api/app/data_store.py`, `api/app/main.py`, `docs/decisions.md`, `docs/STATUS.md` | Built the last 2 planned API endpoints, GET /nowcast and GET /nowcast/{code}/curve, closing the API surface at 11/11. Made nowcast.py's leave_one_year_out_cv return its real out-of-fold predictions (previously discarded after being aggregated into metrics), added best_model_per_lead (honest lowest-MAPE selection per crop x lead), residual_std_by_model (normal-approximation interval from real backtest residuals), and build_curve_table (NDVI/rainfall vs climatology, crop-independent); wired all three into run_nowcast() to write data/public/nowcast.csv and nowcast_curve.csv. Found and fixed a real NaN-to-JSON bug during live verification (a not-yet-surveyed year's null actual_yield_kg_ha crashed the standard JSON encoder because pandas keeps None assigned into a float64 column as NaN) | `src/agritwin/models/nowcast.py`, `src/agritwin/models/run.py`, `api/app/routers/nowcast.py`, `api/app/schemas.py`, `api/app/data_store.py`, `api/app/main.py`, `tests/test_nowcast.py`, `tests/test_models_run.py`, `api/tests/test_nowcast.py`, `api/tests/fixtures/data/public/nowcast.csv`, `api/tests/fixtures/data/public/nowcast_curve.csv`, `docs/decisions.md`, `docs/STATUS.md`, plus refreshed `data/public/nowcast.csv` (1,821 rows) and `data/public/nowcast_curve.csv` (1,260 rows) | Human review documented directly in-session: ran the real `python -m agritwin.models.run` pipeline end to end and confirmed the served model choice per crop x lead time matches the already-validated honest verdict in docs/decisions.md (e.g. ridge for beans, lgbm for sorghum at short leads, baseline_district_mean for maize); ran a real uvicorn against that real regenerated data/public/ and hand-checked both endpoints including the 404 and null-actual edge cases; confirmed all 11 endpoints in /openapi.json; scripts/check_public.py passed; make lint && make test (199 tests total) run clean before commit |
| 2026-09-25 | Claude Code | `web/src/pages/DistrictPage.tsx`, `web/src/pages/DistrictPage.css`, `web/src/lib/scenario.ts`, `web/src/lib/constants.ts`, `web/src/pages/MapPage.tsx`, `web/src/api/hooks.ts`, `web/src/api/types.ts`, `web/src/routes.tsx`, `docs/decisions.md`, `docs/STATUS.md` | Built the web app's second real page, DistrictPage.tsx at /districts/:code, wiring every endpoint that was already built and live-verified server-side but had no UI consumer: /districts/{code}/profile, /scenario/{code} (interactive lever toggles via a new pure, unit-tested lib/scenario.ts), /nowcast (with a lead-time picker), and /briefs/{code}.pdf. MapPage's district panel now links to it. Deduplicated CROPS/CROP_LABELS/SEASONS/MIN_YEAR/MAX_YEAR out of MapPage.tsx into a shared lib/constants.ts. No new npm dependency (ECharts was documented but never actually installed; plain tables suffice for this page's tabular data) | `web/src/pages/DistrictPage.tsx`, `web/src/pages/DistrictPage.css`, `web/src/lib/scenario.ts`, `web/src/lib/scenario.test.ts`, `web/src/lib/constants.ts`, `web/src/pages/MapPage.tsx`, `web/src/App.css`, `web/src/api/hooks.ts`, `web/src/api/types.ts`, `web/src/api/client.ts`, `web/src/routes.tsx`, `docs/decisions.md`, `docs/STATUS.md` | Human review documented directly in-session: tsc/eslint/vitest all pass (13 tests, 3 new); live-verified with a real uvicorn + vite dev server via a directly-scripted headless Chromium (found and killed a stale process squatting on port 5173 from an earlier session that was silently serving stale content without the API proxy); clicked a real district (Gasabo) on the map, followed the new profile link, confirmed all 7 cards render real data including a real early-estimate interval (460-1,035 kg/ha) bracketing the real actual (478 kg/ha), toggled a scenario lever and a lead-time button and confirmed the numbers update live, checked 375px mobile layout, zero browser console errors; npm run build succeeds; make lint && make test clean repo-wide |
| 2026-09-25 | Claude Code | `web/src/i18n/index.ts`, `web/src/i18n/locales/en.json`, `web/src/i18n/locales/fr.json`, `web/src/i18n/locales/rw.json`, `web/src/components/LanguageSwitcher.tsx`, `web/src/pages/MapPage.tsx`, `web/src/pages/DistrictPage.tsx`, `web/src/components/Legend.tsx`, `docs/decisions.md`, `docs/STATUS.md` | Wired i18n (en/fr/rw) across both web pages, using the already-installed-but-unused i18next/react-i18next dependencies. Every hardcoded string in MapPage.tsx, DistrictPage.tsx, and Legend.tsx now routes through t(), closing a gap web/CLAUDE.md rule 10 explicitly required and STATUS.md had repeatedly flagged. Language choice persists in the URL ?lang= (rule 2: shareable filter state) and in localStorage (remembered across visits/navigation without a ?lang= in the link). No new npm dependency added | `web/src/i18n/index.ts`, `web/src/i18n/locales/en.json`, `web/src/i18n/locales/fr.json`, `web/src/i18n/locales/rw.json`, `web/src/components/LanguageSwitcher.tsx`, `web/src/pages/MapPage.tsx`, `web/src/pages/DistrictPage.tsx`, `web/src/components/Legend.tsx`, `web/src/lib/constants.ts`, `web/src/App.css`, `web/src/pages/DistrictPage.css`, `web/src/main.tsx`, `docs/decisions.md`, `docs/STATUS.md` | Human review documented directly in-session: tsc/eslint/vitest all pass (13 tests, unchanged: pure-logic-only convention, no new logic here to test); live-verified with a real uvicorn + vite dev server via headless Chromium, switching EN to FR to RW on the map and confirming every visible string changes, confirming ?lang= appears in the URL, navigating from the map in Kinyarwanda to a district profile and confirming it opened already in Kinyarwanda via the localStorage fallback, switching back to English on the district page, and checking a French mobile view at 375px; zero browser console errors in any check. **Disclosed explicitly, not silently shipped: the Kinyarwanda (rw.json) strings are an AI-authored first draft and have not been reviewed by a native speaker** — flagged in docs/decisions.md for the team's Kinyarwanda-focused reviewer before treating them as final |
| 2026-09-25 | Claude Code | `web/package.json`, `web/package-lock.json`, `docs/decisions.md`, `docs/STATUS.md` | Triaged web/'s 14 npm audit findings one dependency chain at a time rather than a blanket --force. Bumped @typescript-eslint/eslint-plugin+parser (6 to 7) and react-router-dom (6 to 7), both dev-tooling-safe or low-risk API surface, resolving 8 findings. Attempted maplibre-gl 4 to 6 (the one critical finding) and reverted after live verification found it genuinely broken: v6's worker bootstrap uses a runtime import.meta.url pattern Vite cannot statically bundle, so the map silently never renders, confirmed in both dev and a real production build. Deferred both maplibre-gl and the remaining dev-only esbuild/vite/vitest chain with documented reasoning rather than leaving them silently unaddressed | `web/package.json`, `web/package-lock.json`, `docs/decisions.md`, `docs/STATUS.md` | Human review documented directly in-session: tsc/eslint/vitest all pass unchanged (13 tests); live-verified the two kept upgrades with a real uvicorn + vite dev server AND a real production vite build + vite preview (deliberately checking prod separately, since the maplibre-gl break was dev-server-cache-masked on the first pass and only surfaced clearly in a fresh production build): map render, district click, district-panel link, peer-district navigation, and the back-to-map link, zero console errors in both dev and prod. The maplibre-gl revert itself was verified the same way before being finalized, confirming the app was back to its pre-attempt working state exactly (git diff on MapPage.tsx was empty) |
| 2026-09-25 | Claude Code | `api/app/data_store.py`, `docs/decisions.md`, `docs/STATUS.md` | Fixed a flagged inconsistency: the API served district boundaries from data/external/ (raw, unsimplified, ~6.25MB) instead of data/public/ (the already-simplified, already district_code-joined file export/geo.py has built since an earlier session but that never actually reached the API). Removed the runtime crosswalk-join method entirely since the public file already carries district_code. Also corrected a stale STATUS.md line that still listed stg_sas_plot_crop.district_name as unwired when it was already fixed in an earlier session | `api/app/data_store.py`, `api/tests/fixtures/data/public/geo/rwanda_districts_simplified.geojson`, `docs/decisions.md`, `docs/STATUS.md` | Human review documented directly in-session: all 45 API tests plus the full 187-test pipeline suite pass, ruff/mypy clean; live-verified with a real uvicorn (caught and killed a stale instance from earlier in the session still serving the old 5.6MB payload, confirming the fix by direct contrast against a fresh instance) and a real vite dev server: GET /districts dropped to ~190KB with district_code still correctly joined (Bugesera = 57), and the full map-to-profile-to-peer-link-to-back-link flow still works with zero console errors |
| 2026-09-25 | Claude Code | `docs/decisions.md`, `docs/STATUS.md` | Investigated (not fixed) why the sorghum driver model underperforms its naive baseline, an open question logged 2026-09-24. Ran a real scratch analysis against the staged plot-crop data (aggregate summaries only, no raw rows printed, per CLAUDE.md rule 2): ruled out sample size (sorghum's row/district counts match maize's), confirmed sorghum has the lowest target variance of the 4 MVP crops and 4 of its 8 boolean practice features are near-constant (<3% adoption), and ran a controlled hyperparameter sweep against the same GroupKFold CV the production code uses, showing heavier regularization recovers roughly half sorghum's gap and also improves maize's already-positive result (i.e. an untuned-defaults problem, not sorghum-specific). Deliberately did not change production hyperparameters: picking a config from the same CV metric being reported as the honest result would itself be a form of leakage, flagged as the next step rather than done here | `docs/decisions.md`, `docs/STATUS.md` | Human review documented directly in-session: the investigation script and its real numbers are in this session's transcript; the honest conclusion (real, structural finding, not a bug, not fully fixable by a quick tuning pass without a proper nested-CV setup) was written into docs/decisions.md rather than either hiding the negative result or shipping an untested "fix" |
| 2026-09-25 | Claude Code | `infra/Dockerfile.api`, `infra/Dockerfile.web`, `api/requirements.txt`, `infra/nginx.conf.template`, `render.yaml`, `docs/DEPLOY.md`, `docs/decisions.md`, `docs/STATUS.md` | Prepared a real Render deployment. Found and fixed a genuine pre-existing bug in Dockerfile.api: it installed the full pipeline requirements.txt (geopandas, lightgbm, shap, weasyprint, ...), none of which the API actually imports, several needing system libraries the slim base image lacks; scoped to a new api/requirements.txt. Fixed both Dockerfiles' hardcoded ports to respect the platform-assigned $PORT (API via shell-form CMD, web via nginx's envsubst templating). Added SPA client-side-route fallback to the web image, needed now that DistrictPage is a real second route. Wrote render.yaml (a Blueprint for both services) and docs/DEPLOY.md. Explicitly did not attempt to connect a Render account or trigger the actual first deploy: that requires an account login/creation, outside what the assistant can do | `infra/Dockerfile.api`, `infra/Dockerfile.web`, `api/requirements.txt`, `infra/nginx.conf.template`, `render.yaml`, `docs/DEPLOY.md`, `docs/decisions.md`, `docs/STATUS.md` | Human review documented directly in-session: both Dockerfiles were actually built and run locally with real docker build/docker run (Docker was available in this environment), not just written. Confirmed: the API image builds (440MB) and correctly serves /health, /api/v1/meta, and /api/v1/districts (190KB) with $PORT respected; the web image builds, serves the SPA, falls back to index.html on a client-side route while still correctly 404ing a genuinely missing asset, and the VITE_API_BASE build arg was confirmed present in the compiled JS bundle by grepping for the exact URL passed at build time. render.yaml's exact Render-specific field names were not validated against Render's live Blueprint parser (no account access); docs/DEPLOY.md discloses this and gives a fallback if Render's UI rejects the current field name |
| 2026-09-24 | Claude Code | `web/index.html`, `web/src/index.css`, `web/src/App.css`, `web/src/pages/MapPage.tsx`, `web/src/components/Legend.tsx` | Full visual/UX redesign of the web app via the /frontend-design skill: deliberate palette/type token system (Archivo + Public Sans, sage-stone/forest-green/marigold), tab-style controls, gradient legend, click-to-select district panel. Found and fixed two real bugs during live verification: district selection was hover-only (contradicting web/CLAUDE.md's own "click district" rule and not working on touch devices) and the map was using an external demo tileset as its basemap (contradicting the "no external map tiles" rule); also fixed a mobile layout collision between the legend and district panel | `web/index.html`, `web/src/index.css`, `web/src/App.css`, `web/src/pages/MapPage.tsx`, `web/src/components/Legend.tsx`, `docs/decisions.md`, `docs/STATUS.md` | Human review documented directly in-session: Claude-in-Chrome and Playwright's MCP tool were both unavailable in this environment, so a real headless Chromium was installed directly and driven with a small script rather than skipping visual verification; checked desktop render, click-to-select (including a real suppressed district, correctly greyed with its explanation), 375px mobile before and after fixing two real layout bugs found by looking at the screenshots, and keyboard focus confirmed via document.activeElement's computed style, not appearance alone; npm run lint/typecheck/test/build all pass |
| 2026-09-25 | Claude Code | `docs/models/` | Full-repo review found `docs/models/` had only the blank `_template.md`, no filled model cards for any of the three trained models. Wrote all three: attainable yield/yield gap, driver model (LightGBM+SHAP), and nowcast, sourced from `config/settings.yaml`, `artifacts/*_metadata.json`, `data/marts/`, `data/public/`, `tests/`, and the relevant `docs/decisions.md` entries, including the honest sorghum-underperformance and nowcast-null-result findings rather than glossing over them | `docs/models/src/agritwin/models/attainable.py.md`, `docs/models/src/agritwin/models/drivers.py.md`, `docs/models/src/agritwin/models/nowcast.py.md` | Human review documented directly in-session: every factual claim in the cards was cross-checked against the actual metadata files, mart parquet contents, and settings.yaml values, not written from memory |
| 2026-09-25 | Claude Code | `src/agritwin/survey/run.py`, `src/agritwin/models/run.py` | Full-repo review found rice rows (out of MVP scope) reaching `data/public/district_yield.csv` and `yield_gap.csv`, since `estimate_ratio()`/`compute_yield_gap()` run over every crop present in the eligible plots, not just `settings.scope.crops`. Restricted both public exports to MVP crops; re-ran `make marts train` on real data, confirming 181 and 127 rice rows respectively dropped | `src/agritwin/survey/run.py`, `src/agritwin/models/run.py`, `tests/test_survey_run.py`, `tests/test_models_run.py`, `docs/decisions.md` | Human review documented directly in-session: 188 pytest (was 187, plus one new test), ruff/mypy clean; re-ran the real pipeline end to end and verified the crop counts in the regenerated public CSVs and via `scripts/check_public.py` |
| 2026-09-25 | Claude Code | `web/src/pages/`, `web/src/components/`, `web/src/styles/`, `web/src/api/`, `web/src/i18n/` | Built the 5 web pages from `web/CLAUDE.md`'s planned structure that did not yet exist: `EarlyEstimatePage` (honest cross-model nowcast backtest, previously had no UI despite a working `/backtest` endpoint), `ScenarioPage` (full 16-combination lever grid explorer, district-selectable), `MethodologyPage`, `DataPage` (live `/meta` data), and `AboutPage`. Added a `SiteNav` component wired into all 7 pages (including the 2 pre-existing ones, which previously had no way to reach any other page), a shared `styles/shared.css` (deduplicating card/table/nav styles out of `DistrictPage.css`), `useBacktest`/`useMeta` hooks, and `BacktestRow`/`MetaResponse` types. Full English and French copy written directly; Kinyarwanda strings are an AI-authored first draft matching the project's existing disclosed convention for `rw.json`, not yet reviewed by a native speaker | `web/src/routes.tsx`, `web/src/pages/EarlyEstimatePage.tsx`, `web/src/pages/ScenarioPage.tsx`, `web/src/pages/MethodologyPage.tsx`, `web/src/pages/DataPage.tsx`, `web/src/pages/AboutPage.tsx`, `web/src/pages/MapPage.tsx`, `web/src/pages/DistrictPage.tsx`, `web/src/pages/DistrictPage.css`, `web/src/components/SiteNav.tsx`, `web/src/styles/shared.css`, `web/src/api/hooks.ts`, `web/src/api/types.ts`, `web/src/i18n/locales/en.json`, `web/src/i18n/locales/fr.json`, `web/src/i18n/locales/rw.json` | Human review documented directly in-session: `tsc`/`eslint`/`vitest` (13 tests) all pass, production `npm run build` succeeds; live-verified with a real `uvicorn` + `vite` dev server via Claude in Chrome across all 5 new pages plus the 2 existing ones, confirming real API data renders (e.g. `/data` showing the live `/meta` response, `/early-estimate` showing real per-lead backtest numbers with the winning model bolded), zero console errors, and a full English-to-Kinyarwanda language switch on the About page with no missing-translation-key fallbacks; the map page's WebGL canvas did not render in this tool's screenshot capture, confirmed via network request and console logs to be a pre-existing headless/CDP rendering limitation (also hit and worked around in an earlier session), not a regression, since both `/districts` and `/yield-gap` returned 200 with correct data |
| 2026-09-25 | Claude Code | `src/agritwin/features/` (deleted), `src/agritwin/survey/run.py`, `web/package.json` | Deleted the orphaned `features/gee_mart.py` module (its output had no consumer, confirmed a third time, explicit user confirmation obtained before the destructive `git rm`), added a human-readable `geo_name` column to `data/public/district_yield.csv` via a crosswalk lookup verified against real `province_code` data, and re-attempted the `maplibre-gl` v4-to-v6 upgrade with its latest patch (6.11.2) to check whether the previously-found Vite worker-bootstrap incompatibility had been resolved upstream (it had not, confirmed live, reverted to 4.7.1) | `Makefile`, `README.md`, `data/external/gee/feat_district_season_joined.csv` (deleted), `data/public/district_yield.csv`, `docs/STATUS.md`, `docs/decisions.md`, `scripts/moving_nisr_files.py`, `src/agritwin/features/` (deleted), `src/agritwin/survey/run.py`, `tests/test_gee_mart.py` (deleted), `tests/test_survey_run.py`, `web/package-lock.json`, `web/package.json` | Human review documented directly in-session: 187 pytest, ruff/mypy clean, `check_public.py` clean; re-ran the real `survey.run` pipeline and confirmed `geo_name` populates correctly for all three geo levels; the maplibre-gl re-check was verified with a real headless Chromium via raw CDP (the Claude-in-Chrome extension was disconnected this session), confirming the exact same `Worker failed to load` failure as the earlier session's finding, then confirming the map renders correctly again after reverting |
| 2026-09-25 | Claude Code | `src/agritwin/export/static_data.py`, `web/src/api/staticFallback.ts`, `web/src/api/client.ts`, `web/src/api/types.ts`, `docs/models/` | Built the offline static-data fallback `web/CLAUDE.md` documented but never built (checked every one of the 9 API endpoints' router code first: all are plain filters or small compositions over the same files `data_store.py` loads, so the mirror is those same files as JSON); generated `web/src/api/types.ts` from the live OpenAPI schema via a new `npm run gen:types` script, closing a documented drift risk; added the fourth and last missing model card (`scenario.py`) | `src/agritwin/export/static_data.py`, `src/agritwin/export/run.py`, `tests/test_export.py`, `web/src/api/staticFallback.ts`, `web/src/api/staticFallback.test.ts`, `web/src/api/client.ts`, `web/src/api/types.ts`, `web/src/api/schema.ts`, `web/package.json`, `web/public/static-data/*.json`, `docs/models/src/agritwin/models/scenario.py.md`, `docs/decisions.md`, `docs/STATUS.md` | Human review documented directly in-session: 191 pytest, ruff/mypy clean; `tsc`/`eslint`/`vitest` (18 tests) and `npm run build` clean. Found and fixed a real bug in the first version of the fallback via live testing, not inspection: the initial implementation only caught network-level fetch failures, missing the more realistic same-origin/no-backend case (a non-throwing, non-JSON response), fixed by also checking response content-type. Live-verified both failure modes with a real headless-browser run: an absolute unreachable API host (true network failure) and the default relative-path/no-backend case, confirming the map page and the composite `/districts/{code}/profile` page (yield trends, top drivers, peer districts) both render correctly from the offline mirror with zero console errors. One overstated claim in the scenario.py model card's validation section was caught and corrected before finalizing, against the actual test file rather than from memory |
