# AI usage log

Required by NISR hackathon rule 10 (AI use must be disclosed). Append one row per work session using `/log-ai-use`. Never delete rows.

**Note on rows marked "reconstructed from git history":** rule 9 asks for one row per AI-assisted
session, logged at the time via `/log-ai-use`. That did not happen for any session before
2026-09-23's compliance pass. Every row below covering work up to and including the district
crosswalk was backfilled in one pass on 2026-09-23 by reading `git log` and this session's own
record, not logged live session-by-session as intended. Going forward (see `CONTRIBUTING.md`),
`/log-ai-use` should be run at the end of each AI-assisted session instead.

| Date | Tool | Area | Task | Files touched | Human review |
|---|---|---|---|---|---|
| 2026-09-23 | Claude Code | `src/agritwin/gee/`, `data/external/boundaries/` | Initial project scaffold: pyproject, Makefile, config loaders, GEE district boundary fetch and extraction skeleton (reconstructed from git history) | `pyproject.toml`, `Makefile`, `config/*.yaml`, `src/agritwin/config.py`, `src/agritwin/gee/boundaries.py`, `src/agritwin/gee/extract.py` | Reconstructed from git history: commit message and diff reviewed by the author before merge (implied by direct commits to `main`, no PR queue per `CLAUDE.md`'s development method) |
| 2026-09-23 | Claude Code | `src/agritwin/gee/` | Fixed two real GEE bugs: GAUL property renaming before `getInfo()`, and using `Reducer.mean()` not `Reducer.sum()` for rainfall zonal stats (reconstructed from git history; commit messages document the bug and fix directly) | `src/agritwin/gee/boundaries.py`, `src/agritwin/gee/extract.py` | Reconstructed from git history: both fixes are regression-tested (`tests/test_gee_boundaries.py::test_raw_gaul_property_names_are_rejected`, `tests/test_gee_extract.py::test_rainfall_spatial_reducer_is_mean_not_sum`) |
| 2026-09-23 | Claude Code | `docs/` | Filled in team member names and contact emails in `ORIGINALITY.md`/`AI_DISCLOSURE.md` (reconstructed from git history) | `ORIGINALITY.md`, `AI_DISCLOSURE.md` | Reconstructed from git history: team-identifying details, necessarily supplied and confirmed by the team, not invented by the model |
| 2026-09-23 | Claude Code | `src/agritwin/features/` | Joined the four Earth Engine output CSVs into one district x season x year mart (reconstructed from git history) | `src/agritwin/features/gee_mart.py`, `src/agritwin/features/run.py`, `tests/test_gee_mart.py` | Reconstructed from git history: covered by `tests/test_gee_mart.py` (row-count preservation, broadcast join, no-null-keys) |
| 2026-09-23 | Claude Code | `api/` | Scaffolded a minimal FastAPI app with `/health` and `/geo/districts` (reconstructed from git history) | `api/app/main.py`, `api/app/data_store.py`, `api/app/schemas.py`, `api/app/settings.py`, `api/app/routers/geo.py`, `api/tests/test_geo.py` | Reconstructed from git history: verified working live in this session's own status audit (`uvicorn` started, both endpoints returned real data) |
| 2026-09-23 | Claude Code | `web/` | Scaffolded a React + Vite + TypeScript app with a MapLibre district map (reconstructed from git history) | `web/src/App.tsx`, `web/src/main.tsx`, `web/package.json`, `web/tsconfig.json`, `web/vite.config.ts` | Reconstructed from git history: **not fully reviewed/fixed** — this session's status audit (`docs/STATUS.md`) found `tsc`, `eslint` and `vitest` all currently fail against this scaffold; flagged as technical debt, not yet corrected |
| 2026-09-23 | Claude Code | `docs/data/variable_audit.md`, `config/sas_variable_map.yaml`, `docs/decisions.md` | `/audit-sas` runs for all 7 SAS years (2019-2025): column-to-canonical-variable mapping, confidence tagging, crop code counts, weight variable summaries; resolved the 2019 segment-level vs 2020+ plot-level weighting design question (with an explicit `survey-methodologist` subagent review) and the `harvest_kg` (s2q21 vs s2q22) source question via direct cross-tab evidence on real (unprinted, schema/summary-only) microdata | `config/sas_variable_map.yaml`, `docs/data/variable_audit.md`, `docs/decisions.md` | Human review documented directly in-session: multiple explicit user requests to verify subagent involvement was real (not restated model conclusions), to check individual per-year evidence rather than pattern inference, and to confirm the evidence chain between subagent-reviewed and post-review-confirmed statistics — all addressed and corrected in `docs/decisions.md` before being accepted |
| 2026-09-23 | Claude Code | `src/agritwin/harmonize/` | Implemented the harmonize module (join SAS years, rename to canonical schema, stack, confidence sidecar, value-label sidecar); user-identified and fixed a farmer_type/erosion_degree value-label preservation gap; logged the district crosswalk gap as a blocking open item | `src/agritwin/harmonize/*.py`, `tests/test_harmonize.py`, `tests/fixtures/synthetic_sas.py`, `docs/decisions.md` | Human review documented directly in-session: plan proposed and approved before implementation (per `/pipeline-step`); user requested and received confirmation of a real join-collision bug found during test-writing; user requested two additional tests (2019 weight sourcing, Season C exclusion) after initial implementation, both added; `make lint && make test` run and passing before each commit |
| 2026-09-23 | Claude Code | `Makefile`, `src/agritwin/CLAUDE.md`, `tests/`, `scripts/`, `api/app/settings.py` | Collapsed the never-built `ingest/` layer into `harmonize/`; fixed a missing `tests/__init__.py` breaking `pytest`; fixed pre-existing `ruff` errors in `scripts/`; fixed a `mypy` error in `api/app/settings.py` (`ConfigDict` vs `SettingsConfigDict`) | `Makefile`, `src/agritwin/CLAUDE.md`, `tests/__init__.py`, `scripts/check_public.py`, `scripts/test_gee.py`, `api/app/settings.py` | Human review documented directly in-session: each fix run through `make test`/`make lint`/`mypy` and confirmed clean before commit; user requested and confirmed each fix explicitly across three separate messages |
| 2026-09-23 | Claude Code | `docs/STATUS.md` | Read-only project status audit against `docs/AgriTwin_Master_Build_Guide.md` (inventory, pipeline/methodology/model/API/frontend status, compliance and security checks, deploy status, prioritized next steps) | `docs/STATUS.md` | Human review documented directly in-session: every claim cross-checked against the actual repo (file reads, `git log`, live `pytest`/`ruff`/`mypy`/`npm` runs, a live `uvicorn` start); one factual gap flagged explicitly (the audit's own referenced plan document does not exist in this repo) rather than guessed around |
| 2026-09-23 | Claude Code | `data/reference/`, `data/public/geo/`, `src/agritwin/gee/`, `src/agritwin/features/gee_mart.py`, `docs/decisions.md`, `docs/data-sources.md` | Built the SAS `district_code` <-> HDX/GAUL district crosswalk (downloaded from HDX, joined against SAS value labels for all 7 years), re-keyed existing GEE outputs, wired `gee/` extraction to a client-side HDX `FeatureCollection`, closed the open blocking item in `docs/decisions.md` | `data/reference/district_crosswalk.csv`, `data/public/geo/districts_adm2.geojson`, `docs/data-sources.md`, `src/agritwin/gee/boundaries.py`, `src/agritwin/gee/extract.py`, `src/agritwin/gee/run.py`, `src/agritwin/features/gee_mart.py`, `tests/test_district_crosswalk.py`, `tests/test_gee_boundaries.py`, `tests/test_gee_extract.py`, `tests/test_gee_mart.py`, `docs/decisions.md` | Human review documented directly in-session: crosswalk exactness verified against all 7 raw SAS years' value labels (not assumed); HDX extraction path verified with a live Earth Engine call, not just mocked tests; `make lint`/`pytest` run clean before each commit |
| 2026-09-23 | Claude Code | `docs/ai-usage-log.md`, `NOTICE`, `CONTRIBUTING.md` | Compliance backfill: this log, a `NOTICE` file (IP transfer + third-party data licenses), and a `CONTRIBUTING.md` rule requiring `ai-usage-log.md` updates in the same PR going forward | `docs/ai-usage-log.md`, `NOTICE`, `CONTRIBUTING.md` | Human review documented directly in-session: content drafted from facts already established in this repo (hackathon rule 10, licenses recorded in `config/data_sources.yaml` and `docs/data-sources.md`), not invented |
| 2026-09-23 | Claude Code | `src/agritwin/clean/`, `src/agritwin/survey/`, `docs/survey-design.md`, `docs/validation.md`, `docs/decisions.md` | Built `clean/` (yield_kg_ha, qc_flag; a prerequisite the task's own instructions surfaced as missing, confirmed with the team before building rather than assumed) and `survey/` (Taylor-linearized, samplics-based, design-based weighted district/province/national yield estimates), documented real design-variable issues found by reading raw value labels and row frequencies (stratum code instability across years, a 2021 unlabeled code resolved with evidence and team confirmation), and validated the national maize estimate against the real, live-fetched NISR SAS 2025B report | `src/agritwin/clean/*.py`, `src/agritwin/survey/*.py`, `config/settings.yaml`, `docs/survey-design.md`, `docs/validation.md`, `docs/decisions.md`, `tests/test_clean.py`, `tests/test_survey.py`, `tests/test_survey_run.py`, `tests/test_survey_validation.py`, `data/public/district_yield.csv` | Human review documented directly in-session: two explicit AskUserQuestion confirmations before implementation (build `clean/` first; treat 2021's unlabeled stratum code as equivalent to code 10) on points the task itself flagged as "stop and ask if ambiguous"; the official validation figure was fetched live from the real NISR report PDF, not recalled from memory; two real samplics library bugs found and worked around during a live run against the full real dataset, not just synthetic tests; `make lint`/`pytest` (81 tests) run clean before every commit |
| 2026-09-24 | Claude Code | `src/agritwin/gee/periods.py`, `data/external/gee/`, `data/public/drivers.csv`, `data/public/drivers_by_district.csv`, `data/public/nowcast_backtest.csv`, `docs/decisions.md` | External-review Step 1: confirmed and fixed a Season A date-alignment bug in GEE extraction (verified against the real NISR SAS 2025 Season A report before touching code, not assumed), re-pulled Season A satellite data only (scope confirmed with the team before spending Earth Engine quota), re-ran features join, drivers, and nowcast, reported before/after metrics | `src/agritwin/gee/periods.py`, `tests/test_gee_periods.py`, `data/external/gee/ndvi_district_season.csv`, `data/external/gee/rainfall_district_season.csv`, `data/external/gee/nowcast_features_district_season_year.csv`, `data/external/gee/feat_district_season_joined.csv`, `data/public/drivers.csv`, `data/public/drivers_by_district.csv`, `data/public/nowcast_backtest.csv`, `docs/decisions.md` | Human review documented directly in-session: root evidence was a live-fetched primary source (the actual NISR report), not an assumption; a second, self-inflicted bug (missing `gaul_district_code` on new rows, silently dropping half the joined feature mart) was caught by checking row counts against the expected total rather than assuming the re-pull succeeded; re-pull scope (Season A only, skip unaffected steps) explicitly confirmed with the team before spending Earth Engine quota; `make lint`/`pytest` (127 tests) run clean before commit; full before/after comparison reported and logged in `docs/decisions.md` before proceeding to Step 2, per the task's explicit checkpoint |
| 2026-09-24 | Claude Code | `src/agritwin/models/nowcast.py`, `src/agritwin/models/run.py`, `docs/nowcast-feature-timing.md`, `docs/decisions.md` | External-review Step 2: investigated and fixed prior-season-yield leakage in the nowcast, using real NISR publication dates (PDF creation timestamps of the actual SAS 2025A/2025B reports) rather than assuming realistic timing; chose option (b) (per-lead-time realistic lookback) per the team's decision; re-ran the backtest and reported before/after | `src/agritwin/models/nowcast.py`, `src/agritwin/models/run.py`, `tests/test_nowcast.py`, `docs/nowcast-feature-timing.md`, `docs/decisions.md`, `data/public/nowcast_backtest.csv` | Human review documented directly in-session: investigation findings (with real evidence, not estimates) presented and the team chose option (b) explicitly before any code changed; `make lint`/`pytest` (130 tests) run clean before commit; before/after backtest comparison confirmed no crop x lead-time combination flips which model wins |
