# AgriTwin Rwanda — Project Status

Updated 2026-09-25 (i18n pass). Supersedes the 2026-09-23 version of this file, which described `clean/`, `survey/`, and `models/` as missing and estimated MVP completion at 20-25%. That is stale; this version reflects the real, current state, pulled from the actual repo (file listings, `git log`, `pytest`, real pipeline runs), not from memory.

## Summary

67+ commits, all still on a single compressed development arc. The full data pipeline now runs end to end on real data: SAS microdata harmonization, yield/QC cleaning, survey-weighted district estimates (validated against a real published NISR report), satellite feature extraction including climatology and lead-time cutoffs, attainable yield and yield gap, a driver model (LightGBM + SHAP) per crop, and a nowcast model with a leave-one-year-out backtest that now also serves real per-district predictions, not just aggregate metrics. A round of external review earlier this session found and fixed four real correctness bugs in the nowcast (a satellite date-alignment error, feature leakage, and survey-noise contamination of the training target) and reported the results honestly, including where the models do not beat a naive baseline. 188 Python tests and 13 vitest tests pass, `ruff`/`mypy`/`eslint`/`tsc` all clean. **The API now has all 11 planned endpoints, none blocked**: `/nowcast` and `/nowcast/{code}/curve`, the last two, were built by making the nowcast's leave-one-year-out CV return its real out-of-fold predictions (previously only aggregated into metrics) and exporting them plus a new NDVI/rainfall curve table to `data/public/`. `models/scenario.py` and `export/` (simplified GeoJSON + 30 PDF briefs) both exist and are wired to their endpoints. **The web app now has all 7 pages from `web/CLAUDE.md`'s planned structure** (`Home` is `MapPage` itself, per UX rule 1 "Map first", not a separate page): `MapPage` (`/`), `DistrictPage` (`/districts/:code`), and, new this session, `EarlyEstimatePage` (`/early-estimate`, the honest cross-model nowcast backtest, previously an endpoint with zero UI), `ScenarioPage` (`/scenario`, the full 16-combination lever grid for a chosen district), `MethodologyPage`, `DataPage` (live `/meta` data), and `AboutPage`. A `SiteNav` now links all 7 pages together; before this session the 2 existing pages had no way to reach each other except a single back-link, and no page linked anywhere else. `make lint && make test` is green across the whole repo. All 7 pages are wired for i18n (en/fr/rw) via `i18next`/`react-i18next`; language choice persists in the URL `?lang=` and in `localStorage`. **Kinyarwanda strings are an AI-authored first draft, not yet reviewed by a native speaker** (flagged in `docs/decisions.md` 2026-09-25 for the team's Kinyarwanda-focused reviewer). A full-repo review this session also found and fixed two real gaps `STATUS.md` itself had missed: `docs/models/` had only the blank template (no filled model cards for any of the 3 trained models, now written), and rice (out of MVP scope) was silently reaching `data/public/district_yield.csv` and `yield_gap.csv` (now filtered, see `docs/decisions.md` 2026-09-25). Rough MVP completion: **~92%** — the entire data-to-model pipeline is real, tested, and honestly validated, the API surface is fully complete, all 7 planned web pages exist and cover the full data surface end to end in three languages, and model cards now exist; what's left is deployment, Kinyarwanda review, and process items outside the assistant's own execution (questionnaire PDFs from NISR, the user's platform choice for deployment).

## What is built and working

- **`src/agritwin/harmonize/`**: joins all 7 SAS years (2019-2025, Seasons A/B) into `stg_sas_plot_crop`, plus confidence and value-label sidecars.
- **`src/agritwin/clean/`**: adds `yield_kg_ha` and a priority-ordered `qc_flag` (missing data, zero/too-small area, not pure-stand, not SSF, above plausibility cap, outlier, ok).
- **`src/agritwin/survey/`**: Taylor-linearized (`samplics`), design-based weighted estimates at district/province/national x crop x season x year, with a three-tier reliability flag (`ok`/`use_with_caution`/`suppressed`). **Validated against the real NISR SAS 2025B report**: national maize yield 1,400 kg/ha (ours) vs 1,300 kg/ha (official), 7.7% off, inside our own 95% CI (`docs/validation.md`).
- **`src/agritwin/gee/`**: MODIS NDVI, CHIRPS rainfall, ESA WorldCover cropland, iSDAsoil, Rwanda district boundaries, a real NISR district_code <-> HDX/GAUL crosswalk, long-run climatology (MODIS 2001-2018, CHIRPS 1991-2020), and lead-time (partial-season) extraction for the nowcast. A real Season A date-alignment bug (satellite data was reading a full season ahead of the actual survey period, confirmed against NISR's own SAS 2025 Season A report) was found and fixed this session; see `docs/decisions.md` 2026-09-24.
- **`src/agritwin/models/attainable.py`**: k-means zoning (no AEZ layer is configured anywhere in this repo, the build guide's own documented fallback) and weighted-90th-percentile attainable yield, plus yield gap.
- **`src/agritwin/models/drivers.py`**: one LightGBM + SHAP model per MVP crop, GroupKFold by district, against a fold-safe naive baseline. 3 of 4 crops beat baseline (irish_potato +20.0%, beans +5.4%, maize +5.0%); **sorghum does not (-4.4%)**, reported plainly rather than tuned away.
- **`src/agritwin/models/nowcast.py`**: NDVI/rainfall anomaly + realistic prior-season-yield feature (which season is used depends on lead time and whether NISR would actually have published it by then, per `docs/nowcast-feature-timing.md`), leave-one-year-out CV, MAPE and MAE reported for two naive baselines plus ridge and LightGBM. **Honest verdict**: no crop beats the naive district-mean baseline by a margin large relative to the year-to-year spread; sorghum (LightGBM) and beans (ridge) show a real but modest, noisy directional edge, maize and irish potato do not (`docs/decisions.md` 2026-09-24). As of this session, the same leave-one-year-out CV also returns its real per-row out-of-fold predictions (previously discarded after being aggregated into metrics), written to `data/public/nowcast.csv` using whichever model actually won each crop x lead time's backtest, plus `data/public/nowcast_curve.csv` (NDVI/rainfall vs climatology per district x season x year x lead_months).
- **`src/agritwin/models/scenario.py`**: precomputed lever grid for the scenario explorer. Per district x crop, predicts mean yield under all 16 combinations of the 4 binary levers in `drivers.levers` (improved seed, inorganic fertilizer, organic fertilizer, irrigation), with a bootstrap 95 percent interval, using the already-fitted driver model. Wired into `run_drivers()`, writes `data/public/scenario.csv` (1,904 rows: ~30 districts x 16 configs x 4 crops). Labelled model-based not causal. Not yet exposed via an API endpoint (`GET /scenario/{code}` is unimplemented, same as before).
- **`data/public/`**: `district_yield.csv`, `yield_gap.csv`, `district_zones.csv`, `drivers.csv`, `drivers_by_district.csv`, `nowcast_backtest.csv`, `scenario.csv`, `geo/districts_adm2.geojson` (an HDX-sourced pipeline input, not an export output), `geo/rwanda_districts_simplified.geojson`, `briefs/{district_code}.pdf` (30 one-page district briefs), plus, new this session, `nowcast.csv` (1,821 rows: per-district-crop-season-year-lead early estimates) and `nowcast_curve.csv` (1,260 rows: per-district-season-year-lead NDVI/rainfall vs climatology). All pass `scripts/check_public.py`.
- **`src/agritwin/export/`**: `geo.py` (boundary simplification + district_code join) and `briefs.py` (one-page PDF per district: each MVP crop's latest yield gap and top-3 SHAP drivers, via jinja2 + weasyprint). Wired into `make export`. Deliberately does not take over `survey/`'s and `models/`'s existing direct-to-`data/public/` writes; only adds what didn't exist yet.
- **API** (`api/app/`): `GET /health`, `GET /api/v1/meta`, `GET /api/v1/kpis`, `GET /api/v1/districts` (now also carries the NISR `district_code` alongside `gaul_district_code`, joined server-side from `data/reference/district_crosswalk.csv`, see `docs/decisions.md` 2026-09-24), `GET /api/v1/districts/{code}/profile`, `GET /api/v1/yield-gap`, `GET /api/v1/drivers`, `GET /api/v1/backtest`, `GET /api/v1/scenario/{code}`, `GET /api/v1/briefs/{code}.pdf`, and, new this session, `GET /api/v1/nowcast` and `GET /api/v1/nowcast/{code}/curve` (crop/season/year filtered where applicable, every row including `"suppressed"` ones returned with a reliability flag rather than silently dropped, per `CLAUDE.md` golden rule 6). **All 11 planned endpoints now implemented, none blocked.** Live-verified with a real `uvicorn` run against real `data/public/`, not just unit tests. A real `DATA_DIR` semantics bug (the real `.env` config pointed at the wrong subfolder) was found and fixed earlier via this live check; a real NaN-to-JSON encoding bug (a not-yet-surveyed year's null `actual_yield_kg_ha`) was found and fixed while building `/nowcast` this session.
- **Compliance**: no raw microdata, secrets, or credentials anywhere in git history (checked across all commits, not just recent ones). `docs/ai-usage-log.md` now has real entries for every session, not just a header row. `NOTICE` (IP transfer + third-party licenses) exists. `CONTRIBUTING.md` exists.
- **Docs**: `docs/decisions.md` (23 dated entries), `docs/survey-design.md`, `docs/data-sources.md`, `docs/validation.md`, `docs/nowcast-feature-timing.md`, `docs/data/variable_audit.md`, `AI_DISCLOSURE.md`, `ORIGINALITY.md`.

## What is partial or broken

- ~~`stg_sas_plot_crop.district_name`: still null~~ — fixed this session. `harmonize/run.py` now joins `data/reference/district_crosswalk.csv` onto the stacked table; re-ran `make stage` on real 2019-2025 data, all 513,369 rows across 30 districts got a non-null name, zero unmatched. `survey/`'s public `geo_code` is still a raw numeric code, not a label — a separate, smaller follow-up if the API/web layer ever wants district names straight from `survey/` output rather than joining `/districts`' `district_code` client-side.
- **API**: all 11 endpoints implemented and live-verified, nothing outstanding.
- **Web app**: `tsc`, `eslint`, `vitest`, and `npm run build` all pass. `pages/MapPage.tsx`, a district yield-gap choropleth (crop/season/year controls, click-to-select district panel, low-reliability cells shown grey, gradient legend, "Model-based estimate" badge), routed via `react-router-dom` and backed by TanStack Query hooks in `api/hooks.ts`. Given a full visual/UX redesign in an earlier session (see `docs/decisions.md` 2026-09-24): a deliberate design system (palette, Archivo + Public Sans typography, tab/stepper controls), click-to-select district selection, and a plain inline map basemap (no external tiles). **New this session**: a second real page, `pages/DistrictPage.tsx` (`/districts/:code`), a card-grid detail view wired to every previously-server-only-verified endpoint: `/districts/{code}/profile` (yield trend, gap trend, top SHAP drivers, peer districts), `/scenario/{code}` (interactive lever toggles, pure client-side lookup via the new `lib/scenario.ts`, unit tested per `web/CLAUDE.md`'s own instruction), `/nowcast` (early estimate with a lead-time picker), and `/briefs/{code}.pdf` (download link). `MapPage`'s district panel now links to it. **Both pages now wired for i18n** (see `docs/decisions.md` 2026-09-25): every hardcoded string routed through `t()`, a `LanguageSwitcher` in both mastheads, language persisted in the URL `?lang=` and `localStorage`. English and French are high-confidence; Kinyarwanda is an AI-authored first draft pending native-speaker review. Live-verified with a real `uvicorn` + `vite`, desktop and 375px mobile, all 3 languages, zero console errors. **New this session** (`docs/decisions.md` 2026-09-25): `EarlyEstimatePage`, `ScenarioPage`, `MethodologyPage`, `DataPage`, and `AboutPage`, the remaining pages from `web/CLAUDE.md`'s planned structure, plus a `SiteNav` component linking all 7 pages together. `npm install` succeeds locally. `react-router-dom` bumped to v7 and `@typescript-eslint/*` to v7 this session (see `docs/decisions.md` 2026-09-25), reducing `npm audit` from 14 to 6 findings; a `maplibre-gl` v4-to-v6 bump was attempted for the one critical-severity finding and reverted after it broke the map's web worker in both dev and a real production build (a genuine current maplibre-gl/Vite incompatibility, not a usage mistake; low real exploitability for this app since no untrusted HTML ever reaches MapLibre's sanitizer). Re-attempted 2026-09-25 with the latest patch release (6.11.2, which does fix the underlying XSS advisory) specifically to check whether the incompatibility had since been resolved upstream: confirmed live in a real headless-browser run that it has not (`Worker failed to load` console errors, blank map canvas), so this remains a genuine, re-verified blocker, not a stale finding. 6 vulnerabilities remain: `maplibre-gl` (deferred, re-confirmed) and the dev-only `esbuild`/`vite`/`vitest` chain (deferred: dev-server-only exposure, a major `vite` bump risks the same class of breakage just seen).
- **`docs/data/questionnaires/`**: still empty.

## What is missing vs. the plan

| Module | Status |
|---|---|
| ~~`models/scenario.py` — precomputed lever grid~~ | Done, `data/public/scenario.csv`, `GET /scenario/{code}` |
| ~~`export/` — PDF briefs, simplified GeoJSON~~ | Done, `GET /briefs/{code}.pdf` |
| ~~`models/nowcast.py` per-district predictions + curve export~~ | Done, `data/public/nowcast.csv`, `data/public/nowcast_curve.csv`, `GET /nowcast`, `GET /nowcast/{code}/curve` |

Nothing left in this table: every module and endpoint the build guide's API surface lists now exists.

`survey/run.py` and `models/run.py` still write their own outputs directly to `data/public/`; `export/` was deliberately built to add the two new things (simplified boundaries, PDF briefs) on top of that pattern rather than replace it (see `docs/decisions.md` 2026-09-24 for the reasoning). This is now a settled decision, not an open one.

## Compliance and security findings

1. **No raw microdata in git history.** Re-confirmed across all 66 commits, not just the ones since the last audit. **Clean.**
2. **No secrets in git history.** Re-confirmed. **Clean.**
3. **AI disclosure**: `docs/ai-usage-log.md` now has a real entry per session (previously flagged as empty). `AI_DISCLOSURE.md` and `ORIGINALITY.md` remain substantive.
4. **`NOTICE` now exists** (IP transfer to NISR + third-party data licenses), resolving the previous audit's finding.
5. **`LICENSE` now exists** (MIT, confirmed with the team; the hackathon's own IP-transfer rule was independently checked live against the actual NISR hackathon page and imposes no constraint on which license the repo displays before submission), resolving the previous finding.
6. **No code that looks copied from elsewhere**, same finding as before, re-confirmed against everything built since.
7. **`web/` npm audit**: triaged this session (`docs/decisions.md` 2026-09-25). 8 of 14 findings fixed (`react-router-dom` and `@typescript-eslint/*` bumped). 6 remain, both deliberately deferred with documented reasoning: `maplibre-gl` (critical; a v6 upgrade breaks the map's web worker under Vite, re-verified against the latest 6.11.2 patch on 2026-09-25 with a real headless-browser run, still broken; low real exploitability here since no untrusted HTML reaches the library) and the dev-only `esbuild`/`vite`/`vitest` chain (moderate/high; dev-server-only exposure, a major `vite` bump carries real breakage risk).

## Technical debt and quick wins

- ~~`web/tsconfig.json`, Vite client types, ESLint config, and test files~~ — fixed this session.
- ~~`stg_sas_plot_crop.district_name` should be wired from the crosswalk~~ — already fixed in an earlier session (`harmonize/run.py` joins `district_crosswalk.csv`); this bullet was stale, not an open item. Corrected 2026-09-25.
- ~~`features/gee_mart.py` should join the SAS-side district estimates now that the crosswalk exists~~ — resolved 2026-09-25: deleted instead. Confirmed a second time that its output had no consumer anywhere in the repo (`models/run.py` already reads and joins GEE features itself); building a speculative SAS-side join would have been dead code from day one, so the module, its test, the `make features` step, and the tracked `feat_district_season_joined.csv` output were all removed rather than deferred a third time.
- `docs/data/questionnaires/` is still empty; depends on NISR providing the questionnaire PDF. (M, external dependency)
- ~~`api/app/data_store.py` loaded district boundaries from `data/external/` directly rather than `data/public/`~~ — fixed 2026-09-25 (`docs/decisions.md`): now serves `data/public/geo/rwanda_districts_simplified.geojson` (already `district_code`-joined at export time), removing a runtime crosswalk join and cutting the `/districts` response from ~5.6MB to ~190KB.
- The driver model's `sorghum` underperformance and the nowcast's largely-null-result verdict are not technical debt to "fix" by tuning; they are honest findings that should be carried into any model card or UI copy rather than glossed over.

## Recommended next tasks, ordered by judging-impact

Judging: Problem relevance, Data and methodology, Tech innovation, Usability, Tangible impact (20 pts each).

1. ~~Wire `/drivers` and `/backtest` API endpoints~~ — done. ~~Ship one real end-to-end page~~ — done (yield-gap map). ~~Wire `district_name` into `stg_sas_plot_crop`~~ — done. ~~`models/scenario.py`~~ — done. ~~Wire `/meta`, `/kpis`, `/districts/{code}/profile`, `/scenario/{code}`~~ — done. ~~`export/`~~ — done. ~~`GET /briefs/{code}.pdf`~~ — done. ~~`/nowcast`, `/nowcast/{code}/curve`~~ — done this session. **The full API surface is now complete: 11/11 endpoints, none blocked.**
2. ~~Ship a second real web page (district detail)~~ — done this session: `DistrictPage.tsx` at `/districts/:code`, wired to `/districts/{code}/profile`, `/scenario/{code}`, `/nowcast`, and `/briefs/{code}.pdf`.
3. **Deploy and smoke-test**: platform chosen (Render). Everything the assistant can do without a Render account login is done and locally verified this session (`docs/decisions.md` 2026-09-25): fixed a real pre-existing bug in `infra/Dockerfile.api` (installed the full pipeline's dependencies, several needing system libraries the slim base image doesn't have; scoped to a new `api/requirements.txt` instead), fixed both Dockerfiles' `$PORT` handling (hardcoded ports don't work on Render), added SPA fallback routing to the web image (needed now that `DistrictPage` is a real second route), and added `render.yaml` plus `docs/DEPLOY.md`. Both images were actually built and run locally with real `docker build`/`docker run`, not just written. **Not yet done: the actual Render account connection and first deploy**, which needs the user to log into Render and apply the Blueprint (`docs/DEPLOY.md` has the exact steps) — an account action outside what the assistant can perform. *Tangible impact.* — **S** remaining (was **M**; the hard part is done)
4. ~~Investigate why sorghum's driver model and the nowcast overall don't beat baseline~~ — answered this session (`docs/decisions.md` 2026-09-25), not just re-asserted: sample size ruled out (sorghum's row/district counts match maize's); confirmed with real numbers that sorghum's target has the lowest variance of the 4 crops and 4 of its 8 practice features are near-constant (<3% adoption); confirmed with a controlled sweep that heavier regularization recovers roughly half the gap (-5.0% to -1.35%) without being sorghum-specific (it also improves maize's already-positive result). **Deliberately not applied to production**: picking a hyperparameter config from the same CV metric being reported as the honest result would be leakage; a proper fix needs nested CV, flagged as the next step, not done here. Nowcast's parallel question was already investigated in 4 earlier passes; what's left is structural (only 6-7 held-out years per crop), not a quick fix.
5. **`docs/data/questionnaires/`**: request the SAS questionnaire PDFs from NISR to independently confirm the 2025 low-confidence column matches and the 2021 stratum-code resolution. *Data and methodology.* — **M**, external dependency
6. ~~Wire i18n~~ — done this session (en/fr/rw on both pages); Kinyarwanda strings need native-speaker review before final. ~~`web/` npm audit~~ — triaged this session, 8/14 fixed, 6 deliberately deferred with documented reasons. ~~`api/app/data_store.py` boundary source~~ — fixed this session, now serves `data/public/` and is ~30x smaller. ~~`features/gee_mart.py`'s orphaned output~~ — deleted. ~~`survey/`'s raw numeric `geo_code`~~ — `district_yield.csv` now also carries a human-readable `geo_name` column alongside it.
